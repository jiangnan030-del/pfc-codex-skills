# External Mesh Helper

The compiled `create_mesh.exe` helper is excluded from the public skill package. Public workflows should use source-controlled mesh-generation scripts or document the external tool as a user-provided dependency.
